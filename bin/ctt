#!/bin/sh

# (ctt='ct ${1?} template ${2}' uses ../latex/template.tex) as the template:
# 1:out 2:cheetsheet
# example ctt stokesTheoremGeometricAlgebra [ts]
#
# results in something like:
# ct luExample template
#
ct ${1?} template ${2}
